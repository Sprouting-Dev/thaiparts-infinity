{
  "collectionName": "components_shared_ctas",
  "info": {
    "displayName": "cta",
    "description": "Call-to-action button with 4 variants: hero-secondary (black with gradient border), secondary (red with gradient border), primary (blue with gradient border), content-primary (large blue with gradient border)"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "href": {
      "type": "string",
      "required": true
    },
    "variant": {
      "type": "enumeration",
      "required": true,
      "default": "secondary",
      "enum": ["hero-secondary", "secondary", "primary", "content-primary"]
    },
    "newTab": {
      "type": "boolean",
      "required": false,
      "default": false
    },
    "enabled": {
      "type": "boolean",
      "required": false,
      "default": true
    }
  },
  "config": {}
}
